/**
 * styled.ts
 * Commonly used exports for styled components.
 */

// Node Modules
import {createGlobalStyle} from 'styled-components';

// Global Style
export const GlobalStyle = createGlobalStyle`
  body {
    margin: 0px;
  }

  ul {
    list-style: none;
    margin: 0px;
    padding: 0px;
  }
`;
