/**
 * Theme.tsx
 * Route for updating theme on settings page.
 */

// Node Modules
import { FC } from 'react';

const Theme: FC = () => (
  <div>
    <h1>Theme Settings</h1>
  </div>
);

export default Theme;
